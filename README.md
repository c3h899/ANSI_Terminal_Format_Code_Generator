# ANSI_Terminal_Format_Code_Generator
Run time library to generate ANSI terminal format codes.

Parses strings containing format descriptors using:
    ctrm::format_code(string)

See ansi_term.hpp for format codes
